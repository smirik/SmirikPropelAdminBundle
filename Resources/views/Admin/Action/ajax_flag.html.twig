<td class="center propel-admin-action">
    <a data-url="{{ path(action.route, { 'id': item.id, 'data': action.value(item), 'setter': action.setter, 'status': action.value(item) }) }}" data-target="#object{{ item.id }}" data-ajax="true" class="btn input-block-level" href="{{ path(action.route, { 'id': item.id, 'data': action.value(item), 'setter': action.setter, 'status': action.value(item) }) }}">
        {{ action.view(item)|trans }}
    </a>
</td>
